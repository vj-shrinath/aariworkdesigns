import type { Metadata } from 'next'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import CompliancePage, { styles } from '@/components/CompliancePage'; import OrdersClient from '@/components/OrdersClient'; import type { Locale } from '@/lib/i18n'; import { complianceMetadata } from '@/lib/compliance';
export const runtime='edge'; export function generateMetadata({params}:{params:{locale:Locale}}):Metadata{return complianceMetadata('Order History','View your AARI Work Designs payments, order status, invoices, and digital access.',`/${params.locale}/orders`)}
import PushSubscriptionButton from '@/components/PushSubscriptionButton';

export default function OrdersPage({params}:{params:{locale:Locale}}){return <><Header/><CompliancePage locale={params.locale} eyebrow="Your account" title="Order history" intro="Sign in to view purchases and payment records associated with your AARI Work Designs account."><section className={styles.section}><div style={{marginBottom: '2rem', padding: '1.5rem', backgroundColor: 'var(--surface-light)', borderRadius: '12px'}}><h3 style={{marginBottom: '1rem', fontSize: '1.1rem', fontWeight: 600}}>Notification Preferences</h3><p style={{marginBottom: '1rem', fontSize: '0.9rem', color: 'var(--text-secondary)'}}>Get notified about new designs and exclusive offers directly on your device.</p><PushSubscriptionButton /></div><OrdersClient/></section></CompliancePage><Footer/></>}
