import type { Metadata } from 'next'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import CompliancePage from '@/components/CompliancePage'; import type { Locale } from '@/lib/i18n'; import { complianceMetadata } from '@/lib/compliance';
export const runtime='edge'; export function generateMetadata({params}:{params:{locale:Locale}}):Metadata{return complianceMetadata('Cancellation Policy','Cancellation rules for AARI Work Designs accounts, subscriptions, and digital orders.',`/${params.locale}/cancellation-policy`)}
export default function CancellationPage({params}:{params:{locale:Locale}}){return <><Header/><CompliancePage locale={params.locale} eyebrow="Payments" title="Cancellation Policy" intro="These rules explain how cancellations work for digital access and subscriptions." sections={[{heading:'Subscriptions',paragraphs:['If subscription plans are enabled for your account, you may request cancellation before the next renewal through the account controls or by contacting support. Cancellation normally stops future renewals; access already granted remains subject to the plan terms.']},{heading:'Orders and digital downloads',paragraphs:['A completed digital order cannot normally be cancelled after access or download begins. Duplicate or technically failed orders may be reviewed under the Refund Policy.']},{heading:'Payment still processing',paragraphs:['If a payment is pending, do not repeat the purchase immediately. Check the payment status and contact support if the status does not resolve.']}]}/><Footer/></>}
