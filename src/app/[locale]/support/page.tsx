import type { Metadata } from 'next'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import CompliancePage, { styles } from '@/components/CompliancePage'; import ContactForm from '@/components/ContactForm'; import type { Locale } from '@/lib/i18n'; import { complianceMetadata } from '@/lib/compliance';
export const runtime='edge'; export function generateMetadata({params}:{params:{locale:Locale}}):Metadata{return complianceMetadata('Support','Payment, download, account, and technical support for AARI Work Designs.',`/${params.locale}/support`)}
export default function SupportPage({params}:{params:{locale:Locale}}){return <><Header/><CompliancePage locale={params.locale} eyebrow="Help centre" title="Support" intro="Find practical help for payments, downloads, accounts, and the tracing tools."><div className={styles.grid}><div className={styles.card}><h3>Payment issues</h3><p>Check the payment status before retrying. If money was debited but the order is not confirmed, email your order ID.</p></div><div className={styles.card}><h3>Download issues</h3><p>Sign in with the purchase email, refresh the order page, and contact support if access is still missing.</p></div><div className={styles.card}><h3>Account issues</h3><p>Use the password reset option from the account dialog. Never share your password with support.</p></div><div className={styles.card}><h3>Self-service</h3><p>See our <a className={styles.link} href={`/${params.locale}/faq`}>FAQ</a> and <a className={styles.link} href={`/${params.locale}/refund-policy`}>Refund Policy</a>.</p></div></div><ContactForm/></CompliancePage><Footer/></>}
