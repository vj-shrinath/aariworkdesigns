import type { Metadata } from 'next'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import CompliancePage, { styles } from '@/components/CompliancePage'; import type { Locale } from '@/lib/i18n'; import { complianceMetadata } from '@/lib/compliance';
const faqs=[['How do downloads work?','Digital access is provided instantly after a successful payment. Use the confirmation page or your account area to continue.'],['What is the refund policy?','Digital purchases are generally non-refundable after access begins. Duplicate charges and verified technical failures can be reviewed.'],['Which payment methods are supported?','Checkout is processed by Cashfree and shows the payment methods available for your session.'],['Is there a subscription?','Premium plans may provide access to enhanced tools and are shown in the checkout modal when enabled.'],['Can I use designs commercially?','Check the licence supplied with each resource. Do not resell or redistribute the original files.'],['Do I need an account?','Free tracing tools may be used without an account. An account may be required for premium access and order history.']];
export const runtime='edge'; export function generateMetadata({params}:{params:{locale:Locale}}):Metadata{return complianceMetadata('FAQ','Answers about downloads, payments, subscriptions, licences, and accounts at AARI Work Designs.',`/${params.locale}/faq`)}
export default function FaqPage({params}:{params:{locale:Locale}}){const schema={'@context':'https://schema.org','@type':'FAQPage',mainEntity:faqs.map(([name,text])=>({'@type':'Question',name,acceptedAnswer:{'@type':'Answer',text}}))};return <><Header/><CompliancePage locale={params.locale} eyebrow="Help centre" title="Frequently asked questions" intro="Quick answers about using AARI Work Designs and purchasing digital access."><section className={styles.section}>{faqs.map(([q,a])=><div className={styles.faqItem} key={q}><h3>{q}</h3><p>{a}</p></div>)}</section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></CompliancePage><Footer/></>}
